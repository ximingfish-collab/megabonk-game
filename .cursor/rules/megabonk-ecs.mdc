---
description: MegaBonk ECS 架构约定（game/core/source 目录职责）
globs: game/**/*.ts
alwaysApply: false
---

# ECS 架构（方案 A 重构）

`game/core/source/` 目录职责：

- `index.ts` — 公开 API（锁定签名，慎改导出）
- `GameInstance.ts` — 薄 facade
- `world.ts` — miniplex world
- `data/` — 数据表（武器 / 敌人 / 升级 / 波次）
- `components/` — ECS 数据组件
- `systems/` — 每帧 tick 的纯函数
- `behaviors/` — 行为 ID 注册表
- `factories/` — spawnEnemy / spawnWeapon
- `stats/` — 四层 stat 管线（base/added/increased/more + tag）
- `ai/` — Brain 组件 + boss phase script

重构时优先改 systems / components / data，避免动锁定文件与公开 API。
