---
description: i18n 键必须 en/zh 同步
globs: i18n/**
alwaysApply: false
---

# i18n 同步规则

修改 `i18n/en.json` 或 `i18n/zh.json` 时，**必须**在另一侧添加相同键。

提交前可跑：

```bash
bash scripts/harness/check-contract.sh
```

运行时 `t()` 缺键会显示 key 字符串，导致 UI 异常。
