---
description: MegaBonk 框架契约 — 锁定文件与改动前检查清单
alwaysApply: true
---

# 框架契约（必读）

开始任何代码修改前，先阅读 `docs/contract.md`。

## 锁定文件（不得修改）

`index.html` / `vite.config.ts` / `tsconfig.json` / `pnpm-workspace.yaml` / 根 `package.json` / `template.yml` / `kubee.json` / `packages/*/package.json` / `packages/*/source/**` / `game/*/package.json` / `game/client/main.ts`

Cursor `preToolUse` hook 会硬阻断 `Write` / `StrReplace` 命中上述文件。

## 锁定签名（可改内部，导出不变）

`game/core/source/index.ts` 必须导出：`GameInstance`、`GameState`、`GameConfig`、`GameResult`、`InputState`、`TICK_INTERVAL_MS`、`DEFAULT_GAME_CONFIG`

`GameInstance` 必须保留：`start()`、`tick()`、`applyAction()`、`getState()`、`getResult()`

## 改动前四问

1. 目标文件在锁定列表里吗？→ 停手，开 `[CONTRACT]` Issue
2. 要改 `@minigame/core` 公开导出吗？→ 改完跑 `bash scripts/harness/check-contract.sh`
3. 要改 `i18n/*.json` 吗？→ en.json 与 zh.json 必须同步
4. 要新增依赖吗？→ 用 `pnpm add` 加到对应 workspace 包

## 自由区

`game/core/source/` 除 `index.ts` 外的一切；`game/client/source/index.ts`；新增 npm 依赖；新增子目录
